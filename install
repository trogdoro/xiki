if [[ "-bash" = $0 ]]; then
  if [[ -f ./bin/xsh ]]; then
    ./bin/xsh -xsh/setup
  else
    echo "Invoke this file like this:"
    echo -e "\n$ ./install.sh\n"
  fi
else
  # Assume we were executed are not included, so this should work
  $(dirname $0)/bin/xsh -xsh/setup
fi
