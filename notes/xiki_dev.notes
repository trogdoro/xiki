| Summary
Meant to serve as misc notes for development of project.

| Misc todo
- git.rb: move git commands from repository.rb into it
  ../
    - from: repository.rb
      ##Console.+git /
    - to: git.rb

- rake.rb: show list of dirs
  - /projects/xiki/xiki_git/
    - rake.rb
      |  def self.menu task=nil
      |
      |    # If no dir passed, show list (current and after bar)
      |    # Check whether they are rake dirs?
